<h1>Here are all the posts for <%= @author.first_name + " " + @author.last_name %></h1>
<% @posts.each do |post| %>
  <p>
    <%= link_to post.title, post_path(post) %>
  </p>
<% end %>

<p>
  <%= link_to "Add a post", new_author_post_path(@author) %>
</p>